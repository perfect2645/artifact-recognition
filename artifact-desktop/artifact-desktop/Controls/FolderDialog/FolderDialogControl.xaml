<UserControl x:Class="artifact.desktop.Controls.FolderDialogControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:artifact.desktop.Controls"
             x:Name="ctrl">
    <Grid Background="AliceBlue">
        <StackPanel HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
            <Button Content="Select Folder"
                    Width="150" Height="30" Margin="10"
                    Command="{Binding OpenFolderDialogCommand, ElementName=ctrl}"/>
            <TextBlock Text="{Binding SelectedFolderPath, ElementName=ctrl}" VerticalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
