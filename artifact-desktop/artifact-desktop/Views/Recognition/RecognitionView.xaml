<UserControl x:Class="artifact.desktop.Views.Recognition.RecognitionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:artifact.desktop.Views.Recognition"
             xmlns:controls="clr-namespace:artifact.desktop.Controls"
             Height="600"
             MinHeight="300"
             Width="800"
             MinWidth="400"
             MaxWidth="1600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <controls:FolderDialogControl />
            
        </Grid>
    </Grid>
</UserControl>
